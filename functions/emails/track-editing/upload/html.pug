include ../../mixins
+warnings(warnings)
p
  strong Track Edited File Link:
  |
  |
  +frontendLink(`/te/tasks/${task.id}/versions/${Object.keys(task.versions).pop()}/file`)
    | download
  | .
p See details of the task #[+frontendLink(`/te/tasks/${task.id}`) here].