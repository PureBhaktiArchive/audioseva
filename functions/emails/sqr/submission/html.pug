if warnings
  .warnings(style='color:red; font-weight:bold;')
    for warning in warnings
      p= warning
p Current set:
ul(style='font-family: Courier,serif')
  for allotment in currentSet
    li
      | #{ allotment.fileName }
      | #{ allotment.dateGiven }
      | #{ allotment.status }
      | #{ allotment.daysPassed }
      if allotment.fileName == submission.fileName
        strong
          |
          | -- CURRENT SUBMISSION
p
  a(href=allotmentLink) Allot the next set
p
  | Please update your previous submission using this update link: #[a(href=updateLink)= updateLink].
  br
  | Please note &mdash; do not make a new submission using this link.
  | For a new submission, use the link in the allotment email.
  br
p
  b A. Audio File Name:
  |
  | #{ submission.fileName }
mixin annotation(item)
  li
    if item.entireFile
      | Entire file
    else 
      | #{ item.beginning }&ndash;#{ item.ending }
    |
    | - #{ item.type }
    if item.description
      |
      | - #{ item.description }
p
  b B. Unwanted parts to be cut:
ul
  for item in submission.unwantedParts
    +annotation(item)
p
  b C. Sound Issues:
ul
  for item in submission.soundIssues
    +annotation(item)
p
  b D. Sound Quality Rating:
  |
  | #{ submission.soundQualityRating }
p
  b E. Total Duration of the Recording:
  |
  | #{ submission.duration.beginning }&ndash;#{ submission.duration.ending }
p
  b F. Comments:
  |
  | #{ submission.comments }
p
  b G. Filled By:
  |
  | #{ submission.author.name }, #{ submission.author.emailAddress }
